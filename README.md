# Fresh Start

This repository has been reset to a minimal baseline.

The previous manuscript-heavy state was checkpointed before reset:

- Git tag: `big-overhaul-checkpoint`

Current intent:

- start a new project direction from a clean repo
- rebuild only the pieces that still serve the new thesis
- keep the old overhaul recoverable from git history and the tag above

Retained root files:

- `LICENSE`
- `.gitignore`

